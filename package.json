{
  "name": "mijn-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev --host --open",
    "dev:debug": "astro dev --host --open --verbose",
    "build": "astro check && astro build",
    "preview": "astro preview --open",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.3",
    "astro": "^7.1.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
